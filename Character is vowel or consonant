//check wheather given character is consonant or vowel.

#include<stdio.h>
int main()
{
    char ch;
    printf("enter a character");
    scanf("%c",&ch);
    (ch=='a' || ch=='e' || ch=='i' || ch=='o' || ch=='u')? printf("%c is a vowel",ch): printf("%c is a consonant",ch);
}
