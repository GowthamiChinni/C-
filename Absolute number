// wap to print a absolute number for a given number

#include<stdio.h>
int main()
{
    int a;
    printf("enter a number");
    scanf("%d",&a);
    if(a<0)
    {
        a=a*(-1);
    }
    printf("Absolute number is %d\n",a);
}
